import React from "react";
import App from "app";

export default class NavbarStart extends App {
    constructor(props) {
        super(props);
    }
    render() {
        const navbarStart = (
            <div className="navbar-start"></div>
        );
        return navbarStart;
    }
}